\documentclass{article}
\usepackage{lisp}
\input{lisputil}
\lispinterpl{%
(\define \sexp (\cons \+ (\cons :1 (\cons :2 ()))))
(\define \sexpi (\cons \+ (\cons :1 (\cons (\quote \x) ()))))
}
\begin{document}
I evaluate \lispinterpl{(\print \sexp)} to \lispinterpl{(\print (\eval \sexp))}.

\lispinterpl{(\texprint (\let ((\x :1)) (\eval \sexpi)))},\lispinterpl{(\texprint (\let ((\x :100)) (\eval \sexpi)))}.
\end{document}