\documentclass{article}
\usepackage{lisp}
\lispinterpl{(\define \sexp (\cons \+ (\cons :1 (\cons :2 ()))))}
\begin{document}
I evaluate \lispinterpl{(\print \sexp)} to \lispinterpl{(\print (\eval \sexp))}.
\end{document}